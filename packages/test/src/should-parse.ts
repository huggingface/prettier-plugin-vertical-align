// We do not want to write this file, only check that the parser can handle it.
//
// As it will remove the parentheses, the `loc` information will be lost in  the AST on some nodes.

const xy = {
  firstVar:
			!!repoStatus?.bo?.someVar &&
			!!repoStatus?.someStuff &&
			(showsomeStuff &&
			repoStatus.someStuff.someVaar100 !== BLABLAxxxxx.xxxoxoxox),
		secondProperty: [...boooMooo.entries()].map(([path, level]) => ({ path, level })),
}
